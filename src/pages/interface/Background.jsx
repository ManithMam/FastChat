import React from "react";
import { Box } from "@mui/material";

const Background = () => {
  return (
    <Box
      sx={{
        position: "absolute",
        width: "100vw",
        height: "100vh",
        backgroundColor: "#0D0D0D",
      }}
    ></Box>
  );
};

export default Background;
